-- Configuration for Whitelist Mode
local config = {
    mode = "local" -- Change to "cmd"  to use the command-based whitelist or "local" for adding names to a list
}

function getConfig()
    return config
end
